# widget-catalog

A tiny synthetic project used as the target of a readiness gate run. It
fetches a catalog page and returns it, retrying on failure.

- `src/app.txt` — the request handler, written as plain pseudocode so the
  fixture stays language-neutral.
- `CHANGELOG.md` — release notes.
