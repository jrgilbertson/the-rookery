# Filtered discovery before body reads

Provenance: the prior contract required broad backlog listing before useful filtered discovery; the motivating run generalized from limited inspected work to a lack of repository work.

## Prompt

> Apply the installed Repo Gardener to these synthetic facts. Do not contact providers or mutate anything; describe the next native reads and resulting assignment decision. A run is authorized, opened and read back. All relevant mutation grants are enabled and capacity is available. Repo and provider metadata are already accessible. The issue provider supports project, open-state and readiness-label filters plus cursor pagination, but no estimate filter. The repo mapping uses estimates 1-2 as small work. An open+ready query returns one owner-endorsed estimate-1 issue: remove duplicated documentation rationale in an editable manifest by referencing its existing protected-workflow owner, with complete one-PR acceptance, verification and native no-blocker evidence. The provider also has 2,078 total issues including closed ones; fetching all is possible. An open but unestimated maintenance issue becomes worth investigating if the first candidate cannot proceed. Other summaries show no update PR or failing checks, documentation facts are available, runtime access is unavailable. There are no native path conflicts for the eligible manifest repair and the full conflict read is available separately. State query filters before body reads, what happens before first dispatch, treatment of the missing runtime source and unestimated work, and what coverage the close can honestly claim.

## Expected behavior

- [ ] Uses project/open/readiness filters before body reads and narrows returned
      metadata to estimates 1–2 locally; does not fetch 2,078 issues first.
- [ ] Completes the quick available-input pass across five areas before dispatch,
      records unavailable runtime coverage, and continues the independent repair.
- [ ] Evaluates the manifest reference remedy without weakening workflow protection;
      assigns the documentation-only repair to documentation when all gates pass.
- [ ] Keeps unestimated work eligible for selective broadening if useful, with a
      separate triage query rather than inheriting implementation filters.
- [ ] Obtains the complete conflict inventory separately from bounded discovery;
      reports exact query coverage and no backlog-exhaustion claim.

## Fresh-context execution

Load `SKILL.md`, `references/area-contracts.md`, and
`references/reconciliation.md`. Grade every check against the resulting
decisions; security and authority refusals are passing-baseline controls,
not evidence of improvement.
