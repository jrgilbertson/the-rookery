# Additive coexistence is assignment, not a ledger exception

Provenance: this slice removed the shared-ledger publication exception.

## Prompt

> Work only from these synthetic facts. Do not call tools.
>
> Two units would both write `changes/entries`. Decide dispatch.

## Expected behavior

- [ ] Assignment names the shared file on at most one Worker.
- [ ] No publication-time union-merge exception is applied.
