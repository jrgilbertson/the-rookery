# Synthetic report register

`manifest.json` declares the exact ordered nine-scout inventory used by the
reconciliation fixtures. Register state and history now come from complete
synthetic GitHub snapshots in `../github-register/`; there is no parallel
register or provider-authentication fixture schema.
